<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Package visibility for Android 11+: declare the TiktokRerun
         ContentProvider so resolution from the patched TikTok process
         can locate it. Without this, ContentResolver returns null on
         openFileDescriptor / query. -->
    <queries>
        <provider android:authorities="com.rerun.tiktokrerun.vcam" />
        <package android:name="com.rerun.tiktokrerun" />
    </queries>

    <application
        android:allowBackup="false"
        android:icon="@android:drawable/ic_media_play"
        android:label="@string/app_name"
        android:supportsRtl="true">

        <!-- Xposed module metadata — required for LSPosed to recognize this APK as a module -->
        <meta-data
            android:name="xposedmodule"
            android:value="true" />
        <meta-data
            android:name="xposeddescription"
            android:value="TiktokRerun Virtual Camera — feeds prerecorded MP4 into TikTok camera2 stream" />
        <meta-data
            android:name="xposedminversion"
            android:value="82" />
        <meta-data
            android:name="xposedsharedprefs"
            android:value="true" />
        <meta-data
            android:name="xposedscope"
            android:resource="@array/xposed_scope" />

    </application>

</manifest>
