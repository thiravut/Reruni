<?xml version="1.0" encoding="utf-8"?>
<!-- values-night intentionally inherits the parent values/themes.xml so
     the app reads identically in dark and light system modes. The Reruni
     landing palette is dark-only — see values/themes.xml for the rationale. -->
<resources />
